[package]
name = "tap-platform"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "tap platform adapters (input hook/injection, permissions, global hotkeys)."

[dependencies]
tap-core = { path = "../tap-core" }
thiserror = "2"
tracing = "0.1"


